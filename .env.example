# Database Configuration
# These are used for production (e.g., Hostinger)
DB_CLIENT=mysql2
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=tally_church_erp
DB_PORT=3306

# SQLite fallback for local/preview (Default)
# Set DB_CLIENT=mysql2 to enable MySQL
